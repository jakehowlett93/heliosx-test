import { ConsultationForm } from "../features/consultation/components/ConsultationForm";

export const Main = () => {
  return (
    <main className="main">
      <ConsultationForm />
    </main>
  );
};
